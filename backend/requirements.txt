fastapi==0.115.4
uvicorn[standard]==0.32.0
python-dotenv==1.0.1

# Mongo
# motor 3.6 pins pymongo<4.10; keep these aligned.
motor==3.6.0
pymongo==4.9.2

# LangChain + agents
langchain-core==0.3.21
langchain-openai==0.2.10
langchain-mongodb==0.3.0
langgraph==0.2.50
openai==1.55.3

# Imaging (Vision agent decodes frames sent over /snap)
Pillow==11.0.0
# langgraph caps numpy in some lines — let pip resolve.
numpy

# Misc
httpx==0.27.2
websockets==13.1
orjson==3.10.11

# Dataset ingest (scripts/ingest_*.py). Only needed if you re-run the ingest
# pipeline; the committed fixtures under data/fixtures/ work without these.
pyarrow==18.0.0    # parses the MTSamples parquet from Hugging Face
